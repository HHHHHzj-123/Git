# 配置 system-operator 子代理

- 日期：2026-09-18
- 目标：创建 `~/.claude/agents/system-operator.md`；同步更新全局 CLAUDE.md 里的「子代理」一节
- 风险：低
- **最后确认正常的步骤**：第 2 步

---

## 一、开工前的状态

- `C:\Users\HZJ\.claude\agents\` **目录不存在**（动手前确认过，`ls` 返回空）。
- `C:\Users\HZJ\.claude\CLAUDE.md` 存在，其中「子代理」一节写的是：
  > 截至 2026-09-18 该子代理尚未创建（`~/.claude/agents/` 目录不存在）。等创建后再在此处启用。
- 涉及的程序 / 文件：Claude Code 自身的配置文件，不涉及任何其它软件。

---

## 二、步骤记录

| # | 做了什么 | 命令 / 改的文件 | 影响 | 怎么撤销 |
|---|---------|---------------|------|---------|
| 1 | 建 agents 目录并写入子代理定义 | `mkdir` 新建 `C:\Users\HZJ\.claude\agents\`；新建 `agents\system-operator.md` | 多出一个「帮手」。**不装也不影响任何现有功能**，只是遇到装软件/改系统的活时多一个可委派的对象 | 删掉 `C:\Users\HZJ\.claude\agents\system-operator.md`（删后 Claude Code 不再认得这个帮手，其它一切照旧） |
| 2 | 更新全局 CLAUDE.md 的「子代理」一节 | 改 `C:\Users\HZJ\.claude\CLAUDE.md`，把那句「尚未创建」替换成启用说明 | 每次会话开头会自动读到这条，知道有这么个帮手、可以直接派活 | 把那两行改回原文（见下方） |

---

## 三、备份

本次**没有做文件备份** —— 改动 1 是纯新增（原本没有这个文件，不存在覆盖风险）。

改动 2 修改了已有文件 `CLAUDE.md`，原本不存在覆盖风险吗？**存在**，但改的只是一行说明文字，原文已抄录在下方，可直接手改回去：

**CLAUDE.md 中这段的原文（回滚用）：**

```
## 子代理
- 计划中有一个 system-operator 子代理，负责软件安装/卸载、配置修改、系统设置调整和故障排查。
- 截至 2026-09-18 该子代理尚未创建（`~/.claude/agents/` 目录不存在）。等创建后再在此处启用。
```

| 文件名 | 对应步骤 | 它是什么 |
|-------|---------|---------|
| （无） | 1、2 | 均为新增文件或一行文字改动，原文已抄在上面 |

---

## 四、收尾

- **结果**：子代理已就位；`~/.claude/CLAUDE.md` 第 11 条生效
- **生效时机**：子代理列表在会话启动时读取。**当前这个对话框可能要重启后才认得它**，新开的对话框没问题。
- **有没有留下没处理干净的东西**：无
- **下次继续要注意**：删掉 `agents\` 目录下这个文件就等于停用该帮手；若要保留目录本身，别删 `agents\` 文件夹
- **最后确认正常的步骤**：第 2 步
