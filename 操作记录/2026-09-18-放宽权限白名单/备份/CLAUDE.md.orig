# 我的电脑操作规范

适用于所有项目和所有会话。

## 关于我
- 请用中文交流。
- 我不是程序员，请用平实的语言说明每一步在做什么、有什么影响。
- 我的电脑：Windows 11 Pro，日常在 PowerShell 中操作。

## 沟通与执行节奏
1. 执行任何操作前，先说明你打算做什么、为什么、可能有什么影响，等我确认后再动手。
2. 涉及多步骤任务时，先给方案，我确认后再执行。
3. 信息不确定时先问我，不要臆造文件路径、命令参数或软件版本号。

## 软件安装与卸载
4. 优先使用系统自带的 winget；确有必要时再用 choco / npm / pip。
5. 不要从不明来源下载安装包，也不要直接执行网上抓来的安装脚本。

## 系统配置修改
6. 修改注册表或系统关键配置（环境变量、PATH、默认程序关联等）前，先备份，并给出回滚方案。
7. 动手前先说明改的是哪一项、影响范围有多大。

## 删除类操作
8. 不要执行任何删除类命令（rm / rmdir / del / Remove-Item 等），除非我明确要求，并逐条跟我确认。

## 权限
9. 需要管理员权限的操作，先告知我，不要尝试绕过权限限制（例如擅自提权或改动 UAC 设置）。

## 公司财务软件
10. 不要修改任何与公司财务软件（ERP、金蝶、用友等）相关的系统配置，除非我明确指示。

## 权限白名单与确认流程的关系
- `~/.claude/settings.json` 里配置了权限白名单，部分操作不会触发弹窗。
- 但白名单只决定会不会被系统拦住，**不代表可以跳过上面的确认流程**。不弹窗也要先说再做。

## 子代理
11. 已配置一个名为 system-operator 的子代理（`~/.claude/agents/system-operator.md`），专门负责软件安装、卸载、配置修改、系统设置调整和故障排查。遇到这类任务可委派给它；上面这些安全约束对它同样适用。

## 操作记录
- 桌面 git 文件夹下有一个 `操作记录\` 目录（`C:\Users\HZJ\Desktop\Git\操作记录\`），用于记录对本机做的每一次操作，以便出错时按步骤回溯。
- 处理涉及本机改动的任务时，按该目录 `README.md` 里的流程走：先复制 `_模板`，填「开工前的状态」，每步记录「怎么撤销」并做备份。
